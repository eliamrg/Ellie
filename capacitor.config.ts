import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Eliam.Ellie',
  appName: 'Ellie',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
